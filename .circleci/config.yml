version: 2

jobs:
  build:
    docker:
      - image: debian:stretch

    steps:
      - checkout

      - start:
          name: Start
          command: echo Hello, world.

      - test:
          name: Test
          command: ng test